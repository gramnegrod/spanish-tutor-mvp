/**
 * OpenAI Realtime WebRTC NPM Module
 *
 * Simple, powerful library for OpenAI's Realtime API with WebRTC audio streaming.
 *
 * @example Basic Usage
 * ```typescript
 * import { OpenAIRealtimeService } from '@spanish-tutor/openai-realtime-webrtc';
 *
 * const service = new OpenAIRealtimeService({
 *   tokenEndpoint: '/api/token',
 *   voice: 'alloy',
 *   instructions: 'You are a helpful assistant'
 * });
 *
 * await service.connect();
 * await service.sendText('Hello!');
 * ```
 *
 * @example React Hook
 * ```tsx
 * import { useRealtime } from '@spanish-tutor/openai-realtime-webrtc/react';
 *
 * function App() {
 *   const { isReady, sendText, messages } = useRealtime({
 *     tokenEndpoint: '/api/token',
 *     autoConnect: true
 *   });
 *
 *   return (
 *     <div>
 *       <button onClick={() => sendText('Hello!')}>Send</button>
 *       {messages.map(msg => (
 *         <div key={msg.id}>{msg.text}</div>
 *       ))}
 *     </div>
 *   );
 * }
 * ```
 */
export * from './types';
export { OpenAIRealtimeService, type RealtimeServiceConfig, type RealtimeServiceState, type RealtimeServiceEvents } from './core/OpenAIRealtimeService';
export { OpenAIRealtimeService as default } from './core/OpenAIRealtimeService';
/**
 * React Integration
 *
 * Use the '/react' import path for React components and hooks:
 *
 * ```typescript
 * import { useRealtime } from '@spanish-tutor/openai-realtime-webrtc/react';
 * ```
 */ 
//# sourceMappingURL=index.d.ts.map