// This file is kept for compatibility but is no longer used.
// The app now uses Supabase Auth through the AuthContext.
// You can safely remove this file once all references are updated.

export const authOptions = {
  // Deprecated - using Supabase Auth instead
}